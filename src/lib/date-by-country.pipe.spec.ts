import { DateByCountryPipe } from './date-by-country.pipe';

describe('DateByCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new DateByCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
